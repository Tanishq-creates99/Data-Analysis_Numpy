import numpy as np
#.zeros()


zerosArray = np.zeros(10)
#print(zerosArray.astype(int))

#fault is its not integer so we need to convert
#use   .astype(int) function


#------------------------

onesArray = np.ones(10)
#print(onesArray.astype(int))

#------------------------

PrefilledArrray = np.full(10,8)
print(PrefilledArrray)